terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0"
    }
    local = {
      source  = "hashicorp/local"
      version = "~> 2.0"
    }
  }
}

# ── Variables ──────────────────────────────────────────────────────────────────
variable "region" {
  description = "AWS region for all 3 nodes"
  default     = "us-east-1" # N. Virginia (widely enabled by default)
}

variable "instance_type" {
  default = "t3.micro"
}

variable "cluster_size" {
  description = "Number of nodes in this cluster"
  type        = number
  default     = 3

  validation {
    condition     = var.cluster_size >= 3
    error_message = "cluster_size must be >= 3."
  }
}

variable "name_prefix" {
  description = "Optional resource name prefix for parallel isolated runs"
  type        = string
  default     = ""
}

# ── Provider ───────────────────────────────────────────────────────────────────
provider "aws" {
  region     = var.region
  sts_region = "us-east-1"
}

# ── SSH key: generated by Terraform, written to local file ────────────────────
resource "tls_private_key" "ssh" {
  algorithm = "RSA"
  rsa_bits  = 4096
}

resource "local_sensitive_file" "ssh_key" {
  content         = tls_private_key.ssh.private_key_pem
  filename        = "${path.module}/raft-key.pem"
  file_permission = "0600"
}

# ── Three nodes in the same region ────────────────────────────────────────────
module "node" {
  count  = var.cluster_size
  source = "../modules/raft-node"

  node_id        = "node${count.index + 1}"
  instance_type  = var.instance_type
  ssh_public_key = tls_private_key.ssh.public_key_openssh
  key_pair_name  = "raft-quic-same"
  name_prefix    = var.name_prefix
}

# ── Outputs (consumed by deploy.sh) ───────────────────────────────────────────
output "node_ips" {
  description = "Public IPs of cluster nodes"
  value       = module.node[*].public_ip
}

output "node_ids" {
  description = "Logical node IDs"
  value       = [for i in range(var.cluster_size) : "node${i + 1}"]
}

output "instance_ids" {
  description = "EC2 instance IDs"
  value       = module.node[*].instance_id
}

output "ssh_key_file" {
  description = "Path to the generated SSH private key"
  value       = local_sensitive_file.ssh_key.filename
}

output "ssh_user" {
  value = "ec2-user"
}

output "scenario" {
  value = "same-region"
}

output "region_labels" {
  description = "Region label for each node (for report/charts)"
  value       = [for _ in range(var.cluster_size) : var.region]
}
